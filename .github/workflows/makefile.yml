name: Build everything

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
        - name: checkout repo
          uses: actions/checkout@v4

        - name: Tout compiler
          with:
            apt: zsh
          run: make github-build
